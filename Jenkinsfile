pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/ThanHtutPC/docker-simple-app.git'
            }
        }
        // other stages...
    }
}
